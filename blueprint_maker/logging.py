
import logging

logging.basicConfig()
logger = logging.getLogger('blueprint-maker')
logger.setLevel(logging.INFO)
# request_header = logging.getLogger('cloudify.rest_client.http')
# request_header.setLevel(logging.ERROR)
